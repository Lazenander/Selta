# Forbidden mechanics outputs

These two values pass the shared `EvidenceResult` Selta schema but violate the
operational-rule specification. Their payloads vary with a nested response that
the shallow resolved premise does not expose. They are conformance mutations,
not candidate assessment inputs and not results a conforming implementation may
emit.
